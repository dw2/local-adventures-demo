// Generated by CoffeeScript 1.6.2
(function() {
  (function($) {
    var modalOptions;

    modalOptions = {
      cssIn: {
        opacity: 0,
        top: '45%'
      },
      animIn: [
        {
          opacity: 1,
          top: '50%'
        }, 200
      ],
      animOut: [
        {
          opacity: 0,
          top: '55%'
        }, 400
      ]
    };
    $.alert = function(options) {
      var _ref;

      if (typeof options === 'string') {
        options = {
          title: options
        };
      }
      $.extend(options, modalOptions);
      options.type = $.trim("" + ((_ref = options.type) != null ? _ref : '') + " alert");
      return new Skylite(options);
    };
    $.confirm = function(options) {
      $.extend(options, modalOptions);
      options.type = 'confirm';
      if (!((options.actions != null) && Object.keys(options.actions).length > 1)) {
        options.actions = $.extend({
          cancel: (function() {
            return true;
          })
        }, options.actions);
      }
      return new Skylite(options);
    };
    return $.location = function(url) {
      $('body').addClass('busy');
      return document.location = url;
    };
  })(jQuery);

  jQuery(document).ready(function($) {
    var $body, $flash;

    $body = $('body');
    $flash = $('#flash');
    return $('<a>').addClass('btn icon dismiss').text('Dismiss').appendTo($flash).click(function() {
      return $flash.slideUp(400, function() {
        return $flash.remove();
      });
    });
  });

}).call(this);
